# .env.example - agent frontend environment variables template
# Copy this file to .env and set values for your environment.

# Backend API base URL used as fallback when application.yml cannot be loaded
VITE_API_BASE_URL=http://localhost:8080/api/v1

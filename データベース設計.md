# データベース設計書

> 作成日: 2026-05-31
> 対象DB: MySQL 8.x  /  データベース名: `project3`

---

## 目次

1. [概要](#1-概要)
2. [テーブル一覧](#2-テーブル一覧)
3. [ER図](#3-er図)
4. [テーブル詳細定義](#4-テーブル詳細定義)
5. [マイグレーション履歴](#5-マイグレーション履歴)
6. [設計上の決定事項・注意点](#6-設計上の決定事項注意点)

---

## 1. 概要

### システム構成

| 項目 | 内容 |
|------|------|
| DBMS | MySQL 8.x |
| データベース名 | `project3` |
| 文字コード | utf8mb4 |
| 照合順序 | utf8mb4_general_ci |
| マイグレーションツール | Flyway |
| テーブル数 | 17テーブル（flyway管理テーブル除く） |

### テーブルグループ

| グループ | テーブル数 | 説明 |
|----------|-----------|------|
| 認証・ユーザー管理 | 1 | システムログインユーザー |
| 社員管理 | 1 | PCを使用する社員のマスタ |
| PC資産管理 | 6 | PC台帳・取得区分詳細・割り当て・貸出 |
| エージェント収集 | 4 | エージェントアプリが収集するPC情報 |
| レンタル管理 | 2 | ベンダー・レンタル履歴 |
| ソフトウェア管理 | 1 | ライセンス管理マスタ |
| システム管理 | 2 | 操作ログ・コードマスタ |
| Flyway管理 | 1 | マイグレーション管理（システム） |

---

## 2. テーブル一覧

| # | テーブル名 | 論理名 | 現在のレコード数 | 説明 |
|---|-----------|--------|:---:|------|
| 1 | `users` | システムユーザー | 0 | 管理画面ログインユーザー |
| 2 | `employees` | 社員マスタ | 0 | PCを使用する社員情報 |
| 3 | `pc_assets` | PC資産マスタ | 1 | 全PCの台帳（メインテーブル） |
| 4 | `pc_acquisition_purchase` | PC購入詳細 | 0 | 購入PCの詳細情報（1:1） |
| 5 | `pc_acquisition_rental` | レンタル契約（現行） | 0 | 現行レンタル契約（1:1） |
| 6 | `pc_rental_history` | レンタル契約履歴 | 0 | 過去のレンタル契約記録 |
| 7 | `pc_assignments` | PC割り当て履歴 | 0 | PC⇔社員の割り当て履歴 |
| 8 | `pc_loans` | PC貸出管理 | 0 | PCの一時貸出管理 |
| 9 | `rental_vendors` | レンタルベンダー | 0 | レンタル会社・業者マスタ |
| 10 | `agents` | エージェントマスタ | 9 | エージェント端末の登録情報 |
| 11 | `agent_history` | エージェント操作履歴 | 0 | エージェントのイベント記録 |
| 12 | `pc_hardware_info` | ハードウェア情報 | 5 | エージェント収集CPU・メモリ等 |
| 13 | `pc_network_info` | ネットワーク情報 | 5 | エージェント収集NIC・IP情報 |
| 14 | `pc_software_info` | ソフトウェア情報 | 1,000 | エージェント収集インストール済みソフト |
| 15 | `software_master` | ソフトウェアマスタ | 0 | ライセンス保有管理 |
| 16 | `operation_logs` | 操作ログ | 9 | 管理画面での操作記録 |
| 17 | `code_master` | コードマスタ | 15 | システム全体で使用するコード値の一元管理 |

---

## 3. ER図

```
[users]                     [employees]
  id PK                       id PK
  username(UQ)                employee_code(UQ)
  password_hash               full_name
  display_name                department
  role                        position
  ...                         ...
  |                           |         |
  | created_by                | employee_id
  |                           |         |
[pc_loans]                    |    [pc_assignments]
  id PK                       |       id PK
  pc_asset_id FK─────────┐    |       pc_asset_id FK──┐
  employee_id  FK─────────────┘       employee_id  FK──┘
  loan_date                           assigned_at
  expected_return_date                unassigned_at
  actual_return_date
  purpose

[pc_assets] ─────────────────────────────────────────────────
  id PK                    |           |           |
  asset_number(UQ)         |           |           |
  device_name              |           |           |
  acquisition_type         |           |           |
  status                   |           |           |
  assigned_employee_id FK  |           |           |
  hostname                 |           |           |
  agent_number FK          |           |           |
  agent_last_seen          |           |           |
  ...                    (1:1)       (1:1)       (1:N)
       |                  |           |           |
       |        [pc_acquisition  [pc_acquisition  [pc_rental_history]
       |          _purchase]      _rental]         id PK
       |          id PK           id PK            pc_asset_id FK
       |          pc_asset_id FK  pc_asset_id FK   rental_vendor_id FK
       |          purchase_date   rental_vendor_id  contract_number
       |          purchase_price  contract_number   rental_start_date
       |          warranty_expiry rental_start_date rental_end_date
       |          supplier        rental_end_date   return_date
       |                          monthly_fee
       |                          return_date
       |                          rental_vendor_id FK
       |                                    |
       |                          [rental_vendors]
       |                            id PK
       |                            company_name
       |                            contact_name
       |                            ...
       |
  [agents] ──────────────────────────────────────
    agent_number PK      |           |          |
    hostname             |           |          |
    created_at         (1:1)       (1:N)      (1:N)
    updated_at           |           |          |
                [pc_hardware_info] [pc_network  [pc_software_info]
                  id PK            _info]        id PK
                  agent_number FK   id PK        agent_number FK
                  cpu_model         agent_number  software_name
                  cpu_cores         nic_name      version
                  memory_gb         ip_address    publisher
                  disk_gb           mac_address   install_date
                  disk_free_gb      collected_at  collected_at
                  collected_at
                       |
                [agent_history]
                  id PK
                  agent_number FK
                  event_type
                  note
                  created_at

[software_master]           [operation_logs]        [code_master]
  id PK                       id PK                   id PK
  software_name               user_id                 code_type(+idx)
  publisher                   username                code_name
  license_type                operation               code_value
  purchased_count             target_type             code_label
  note                        target_id               description
                              detail                  sort_order
                              ip_address              is_active
                              created_at              created_at
                                                      updated_at
                                                      UQ(code_type, code_value)
```

---

## 4. テーブル詳細定義

---

### 4.1 `users` — システムユーザー

管理画面にログインするユーザーアカウントを管理する。

**カラム定義**

| カラム名 | 型 | NULL | デフォルト | 制約 | 説明 |
|---------|-----|:----:|-----------|------|------|
| `id` | bigint | NO | AUTO_INCREMENT | PK | 内部ID |
| `username` | varchar(100) | NO | — | UNIQUE | ログインID |
| `password_hash` | varchar(255) | NO | — | — | bcryptハッシュ化済みパスワード |
| `display_name` | varchar(100) | NO | — | — | 画面表示名 |
| `role` | varchar(20) | NO | `'VIEWER'` | — | 権限区分 |
| `email` | varchar(200) | YES | NULL | — | メールアドレス |
| `is_active` | tinyint(1) | NO | `1` | — | 有効フラグ（1=有効、0=無効） |
| `last_login_at` | datetime | YES | NULL | — | 最終ログイン日時 |
| `created_at` | datetime | NO | CURRENT_TIMESTAMP | — | 登録日時 |
| `updated_at` | datetime | NO | CURRENT_TIMESTAMP | ON UPDATE | 最終更新日時 |

**role の取りうる値**

| 値 | 権限 |
|----|------|
| `ADMIN` | 全操作可能（ユーザー管理含む） |
| `OPERATOR` | 資産・社員・貸出の登録・編集可能 |
| `VIEWER` | 参照のみ |

**インデックス**

| インデックス名 | カラム | 種別 |
|--------------|--------|------|
| PRIMARY | `id` | 主キー |
| `uq_users_username` | `username` | ユニーク |

---

### 4.2 `employees` — 社員マスタ

PCを使用する社員のマスタデータ。PC割り当て・貸出の参照先として使用される。

**カラム定義**

| カラム名 | 型 | NULL | デフォルト | 制約 | 説明 |
|---------|-----|:----:|-----------|------|------|
| `id` | bigint | NO | AUTO_INCREMENT | PK | 内部ID |
| `employee_code` | varchar(50) | NO | — | INDEX | 社員コード（社員番号） |
| `full_name` | varchar(100) | NO | — | — | 氏名 |
| `department` | varchar(100) | YES | NULL | — | 部署名 |
| `position` | varchar(100) | YES | NULL | — | 役職 |
| `email` | varchar(200) | YES | NULL | — | メールアドレス |
| `phone` | varchar(30) | YES | NULL | — | 電話番号 |
| `location` | varchar(200) | YES | NULL | — | 勤務場所 |
| `is_active` | tinyint(1) | NO | `1` | — | 在籍フラグ（1=在籍中） |
| `created_at` | datetime | NO | CURRENT_TIMESTAMP | — | 登録日時 |
| `updated_at` | datetime | NO | CURRENT_TIMESTAMP | ON UPDATE | 最終更新日時 |

> **注**: V5マイグレーションで `employee_code` のUNIQUE制約を廃止。転籍・再入社を考慮して同一コードの複数登録を許容する設計に変更。

**インデックス**

| インデックス名 | カラム | 種別 |
|--------------|--------|------|
| PRIMARY | `id` | 主キー |
| `idx_employees_code` | `employee_code` | インデックス（UNIQUE廃止後） |

---

### 4.3 `pc_assets` — PC資産マスタ（メインテーブル）

システムの中核テーブル。全PCの台帳情報を保持する。購入・レンタルの区別は `acquisition_type` で管理し、詳細情報は `pc_acquisition_purchase` / `pc_acquisition_rental` に分割する。

**カラム定義**

| カラム名 | 型 | NULL | デフォルト | 制約 | 説明 |
|---------|-----|:----:|-----------|------|------|
| `id` | bigint | NO | AUTO_INCREMENT | PK | 内部ID |
| `asset_number` | varchar(50) | NO | — | UNIQUE | **資産番号**（管理番号。例: PC-00123） |
| `device_name` | varchar(200) | NO | — | — | デバイス名・機種名 |
| `acquisition_type` | varchar(20) | NO | — | — | 取得区分（`PURCHASE` / `RENTAL`） |
| `maker` | varchar(100) | YES | NULL | — | メーカー名 |
| `model_number` | varchar(100) | YES | NULL | — | 型番 |
| `serial_number` | varchar(100) | YES | NULL | — | シリアル番号 |
| `location` | varchar(200) | YES | NULL | — | 設置場所（エージェント送信値） |
| `status` | varchar(20) | NO | `'IN_STORAGE'` | — | ステータス |
| `assigned_employee_id` | bigint | YES | NULL | — | 割り当て社員ID（`employees.id` 参照） |
| `user_name` | varchar(200) | YES | NULL | — | エージェント送信の使用者名（自由テキスト） |
| `hostname` | varchar(200) | YES | NULL | — | PCのホスト名（エージェント自動取得） |
| `agent_number` | varchar(50) | YES | NULL | — | エージェント番号（`agents.agent_number` 参照） |
| `agent_last_seen` | datetime | YES | NULL | — | エージェント最終報告日時 |
| `note` | varchar(2000) | YES | NULL | — | 備考 |
| `created_at` | datetime | NO | CURRENT_TIMESTAMP | — | 登録日時 |
| `updated_at` | datetime | NO | CURRENT_TIMESTAMP | ON UPDATE | 最終更新日時 |

**status の取りうる値**

| 値 | 意味 |
|----|------|
| `IN_STORAGE` | 保管中（デフォルト） |
| `IN_USE` | 使用中 |
| `DISPOSED` | 廃棄済み |

**acquisition_type の取りうる値**

| 値 | 意味 |
|----|------|
| `PURCHASE` | 購入品（`pc_acquisition_purchase` に詳細あり） |
| `RENTAL` | レンタル品（`pc_acquisition_rental` に詳細あり） |

**インデックス**

| インデックス名 | カラム | 種別 |
|--------------|--------|------|
| PRIMARY | `id` | 主キー |
| `uq_pc_assets_number` | `asset_number` | ユニーク |

---

### 4.4 `pc_acquisition_purchase` — PC購入詳細

`pc_assets.acquisition_type = 'PURCHASE'` の資産に対する購入詳細情報。`pc_assets` と1対1の関係。

**カラム定義**

| カラム名 | 型 | NULL | デフォルト | 制約 | 説明 |
|---------|-----|:----:|-----------|------|------|
| `id` | bigint | NO | AUTO_INCREMENT | PK | 内部ID |
| `pc_asset_id` | bigint | NO | — | UNIQUE / FK | 対象PC資産ID（`pc_assets.id`） |
| `purchase_date` | date | YES | NULL | — | 購入日 |
| `purchase_price` | decimal(12,2) | YES | NULL | — | 購入金額（税込） |
| `warranty_expiry` | date | YES | NULL | — | 保証期限 |
| `supplier` | varchar(200) | YES | NULL | — | 購入先（サプライヤー名） |
| `note` | varchar(2000) | YES | NULL | — | 備考 |

**外部キー**

| 制約名 | カラム | 参照先 | ON DELETE |
|--------|--------|--------|-----------|
| `fk_purchase_asset` | `pc_asset_id` | `pc_assets.id` | CASCADE |

---

### 4.5 `pc_acquisition_rental` — レンタル契約詳細（現行）

`pc_assets.acquisition_type = 'RENTAL'` の資産に対する**現在進行中のレンタル契約情報**。`pc_assets` と1対1。過去の契約は `pc_rental_history` に保管。

**カラム定義**

| カラム名 | 型 | NULL | デフォルト | 制約 | 説明 |
|---------|-----|:----:|-----------|------|------|
| `id` | bigint | NO | AUTO_INCREMENT | PK | 内部ID |
| `pc_asset_id` | bigint | NO | — | UNIQUE / FK | 対象PC資産ID（`pc_assets.id`） |
| `rental_vendor_id` | bigint | NO | — | FK | レンタルベンダーID（`rental_vendors.id`） |
| `contract_number` | varchar(100) | YES | NULL | — | 契約番号 |
| `rental_start_date` | date | NO | — | — | **レンタル開始日** |
| `rental_end_date` | date | NO | — | — | **レンタル終了日**（期限アラートに使用） |
| `monthly_fee` | decimal(10,2) | YES | NULL | — | 月額料金（税抜き） |
| `contract_file_path` | varchar(500) | YES | NULL | — | 契約書ファイルパス |
| `return_date` | date | YES | NULL | — | **返却日**（NULL=未返却、入力で返却済み判定） |
| `returned_by` | bigint | YES | NULL | — | 返却処理を行ったユーザーID |
| `created_at` | datetime | NO | CURRENT_TIMESTAMP | — | 登録日時 |
| `updated_at` | datetime | NO | CURRENT_TIMESTAMP | ON UPDATE | 最終更新日時 |

**外部キー**

| 制約名 | カラム | 参照先 | ON DELETE |
|--------|--------|--------|-----------|
| `fk_rental_asset` | `pc_asset_id` | `pc_assets.id` | CASCADE |
| `fk_rental_vendor` | `rental_vendor_id` | `rental_vendors.id` | （制限なし） |

---

### 4.6 `pc_rental_history` — レンタル契約履歴

過去に終了・返却されたレンタル契約の履歴保管テーブル。返却処理時に `pc_acquisition_rental` の内容をコピーする。

**カラム定義**

| カラム名 | 型 | NULL | デフォルト | 制約 | 説明 |
|---------|-----|:----:|-----------|------|------|
| `id` | bigint | NO | AUTO_INCREMENT | PK | 内部ID |
| `pc_asset_id` | bigint | NO | — | FK | 対象PC資産ID（`pc_assets.id`） |
| `rental_vendor_id` | bigint | NO | — | FK | レンタルベンダーID（`rental_vendors.id`） |
| `contract_number` | varchar(100) | YES | NULL | — | 契約番号 |
| `rental_start_date` | date | NO | — | — | 開始日 |
| `rental_end_date` | date | NO | — | — | 終了日 |
| `monthly_fee` | decimal(10,2) | YES | NULL | — | 月額料金 |
| `return_date` | date | YES | NULL | — | 実返却日 |
| `created_at` | datetime | NO | CURRENT_TIMESTAMP | — | 履歴登録日時 |

**外部キー**

| 制約名 | カラム | 参照先 | ON DELETE |
|--------|--------|--------|-----------|
| `fk_rental_hist_asset` | `pc_asset_id` | `pc_assets.id` | CASCADE |
| `fk_rental_hist_vendor` | `rental_vendor_id` | `rental_vendors.id` | （制限なし） |

---

### 4.7 `rental_vendors` — レンタルベンダーマスタ

PCをレンタルしている会社・業者の情報を管理する。

**カラム定義**

| カラム名 | 型 | NULL | デフォルト | 制約 | 説明 |
|---------|-----|:----:|-----------|------|------|
| `id` | bigint | NO | AUTO_INCREMENT | PK | 内部ID |
| `company_name` | varchar(200) | NO | — | — | **会社名**（必須） |
| `contact_name` | varchar(100) | YES | NULL | — | 担当者名 |
| `phone` | varchar(30) | YES | NULL | — | 電話番号 |
| `email` | varchar(200) | YES | NULL | — | メールアドレス |
| `address` | varchar(300) | YES | NULL | — | 住所 |
| `note` | varchar(2000) | YES | NULL | — | 備考 |
| `created_at` | datetime | NO | CURRENT_TIMESTAMP | — | 登録日時 |
| `updated_at` | datetime | NO | CURRENT_TIMESTAMP | ON UPDATE | 最終更新日時 |

---

### 4.8 `pc_assignments` — PC割り当て履歴

どの社員にどのPCが割り当てられていたかの履歴を管理する。`unassigned_at` が NULL のレコードが現在の割り当てを表す。

**カラム定義**

| カラム名 | 型 | NULL | デフォルト | 制約 | 説明 |
|---------|-----|:----:|-----------|------|------|
| `id` | bigint | NO | AUTO_INCREMENT | PK | 内部ID |
| `pc_asset_id` | bigint | NO | — | FK / INDEX | 対象PC資産ID（`pc_assets.id`） |
| `employee_id` | bigint | NO | — | FK / INDEX | 割り当て社員ID（`employees.id`） |
| `assigned_at` | datetime | NO | CURRENT_TIMESTAMP | — | **割り当て日時** |
| `unassigned_at` | datetime | YES | NULL | — | **割り当て解除日時**（NULL = 現在割り当て中） |
| `note` | varchar(2000) | YES | NULL | — | 備考 |
| `created_by` | bigint | YES | NULL | — | 登録したユーザーID |

**外部キー**

| 制約名 | カラム | 参照先 | ON DELETE |
|--------|--------|--------|-----------|
| `fk_assignments_asset` | `pc_asset_id` | `pc_assets.id` | CASCADE |
| `fk_assignments_employee` | `employee_id` | `employees.id` | （制限なし） |

---

### 4.9 `pc_loans` — PC貸出管理

PCを社員に**一時的に貸し出す**際の管理テーブル。`pc_assignments`（正式割り当て）とは別に、出張・研修等の短期貸出を管理する。

**カラム定義**

| カラム名 | 型 | NULL | デフォルト | 制約 | 説明 |
|---------|-----|:----:|-----------|------|------|
| `id` | bigint | NO | AUTO_INCREMENT | PK | 内部ID |
| `pc_asset_id` | bigint | NO | — | FK / INDEX | 対象PC資産ID（`pc_assets.id`） |
| `employee_id` | bigint | NO | — | FK / INDEX | 貸出先社員ID（`employees.id`） |
| `loan_date` | date | NO | — | — | **貸出日** |
| `expected_return_date` | date | YES | NULL | — | 返却予定日 |
| `actual_return_date` | date | YES | NULL | — | **実返却日**（NULL = 未返却） |
| `purpose` | varchar(300) | YES | NULL | — | 貸出目的 |
| `note` | varchar(2000) | YES | NULL | — | 備考 |
| `created_by` | bigint | NO | — | FK | 貸出処理を行ったユーザーID（`users.id`） |
| `created_at` | datetime | NO | CURRENT_TIMESTAMP | — | 登録日時 |

**外部キー**

| 制約名 | カラム | 参照先 | ON DELETE |
|--------|--------|--------|-----------|
| `fk_loans_asset` | `pc_asset_id` | `pc_assets.id` | （制限なし） |
| `fk_loans_employee` | `employee_id` | `employees.id` | （制限なし） |
| `fk_loans_created_by` | `created_by` | `users.id` | （制限なし） |

---

### 4.10 `agents` — エージェントマスタ

Tauriエージェントアプリをインストールした各PCの登録情報。`agent_number` を主キーとし、PC情報収集テーブル群のリレーション起点となる。

**カラム定義**

| カラム名 | 型 | NULL | デフォルト | 制約 | 説明 |
|---------|-----|:----:|-----------|------|------|
| `agent_number` | varchar(50) | NO | — | PK | **エージェント番号**（例: AGT-A1B2C3D4） |
| `hostname` | varchar(200) | YES | NULL | — | PCのホスト名（初回登録時に取得） |
| `created_at` | datetime | NO | CURRENT_TIMESTAMP | — | 初回登録日時 |
| `updated_at` | datetime | NO | CURRENT_TIMESTAMP | ON UPDATE | 最終更新日時 |

> **設計ポイント**: `agent_number` を主キーとすることで、PC資産IDやホスト名の変更に依存しない安定したリレーションを実現している（V4マイグレーションで採用）。

---

### 4.11 `agent_history` — エージェント操作履歴

エージェントの各種イベント（登録・更新・レポート送信等）を記録する。

**カラム定義**

| カラム名 | 型 | NULL | デフォルト | 制約 | 説明 |
|---------|-----|:----:|-----------|------|------|
| `id` | bigint | NO | AUTO_INCREMENT | PK | 内部ID |
| `agent_number` | varchar(50) | NO | — | FK / INDEX | 対象エージェント番号（`agents.agent_number`） |
| `event_type` | varchar(50) | YES | NULL | — | イベント種別（`REGISTERED` / `UPDATED` / `REPORT_SENT`） |
| `note` | varchar(2000) | YES | NULL | — | 補足情報 |
| `created_at` | datetime | NO | CURRENT_TIMESTAMP | — | イベント発生日時 |

**外部キー**

| 制約名 | カラム | 参照先 | ON DELETE |
|--------|--------|--------|-----------|
| `fk_agent_history_agent` | `agent_number` | `agents.agent_number` | （制限なし） |

---

### 4.12 `pc_hardware_info` — ハードウェア情報

エージェントが収集したPCのハードウェア情報。エージェント番号と1対1（UNIQUE制約）のため、エージェントが情報送信するたびに上書き更新される。

**カラム定義**

| カラム名 | 型 | NULL | デフォルト | 制約 | 説明 |
|---------|-----|:----:|-----------|------|------|
| `id` | bigint | NO | AUTO_INCREMENT | PK | 内部ID |
| `agent_number` | varchar(50) | YES | NULL | UNIQUE / FK | 対象エージェント番号（`agents.agent_number`） |
| `cpu_model` | varchar(200) | YES | NULL | — | CPUモデル名（例: Intel Core i7-1165G7 @ 2.80GHz） |
| `cpu_cores` | int | YES | NULL | — | CPU論理コア数（ハイパースレッディング含む） |
| `memory_gb` | decimal(6,1) | YES | NULL | — | 総メモリ容量（GB、小数点1位） |
| `disk_gb` | decimal(8,1) | YES | NULL | — | ディスク総容量（GB、全ドライブ合計） |
| `disk_free_gb` | decimal(8,1) | YES | NULL | — | ディスク空き容量（GB、全ドライブ合計） |
| `collected_at` | datetime | YES | NULL | — | エージェントの情報収集日時 |
| `created_at` | datetime | NO | CURRENT_TIMESTAMP | — | レコード初回登録日時 |
| `updated_at` | datetime | NO | CURRENT_TIMESTAMP | ON UPDATE | 最終更新日時 |

**外部キー**

| 制約名 | カラム | 参照先 | ON DELETE |
|--------|--------|--------|-----------|
| `fk_hardware_agent` | `agent_number` | `agents.agent_number` | CASCADE |

---

### 4.13 `pc_network_info` — ネットワーク情報

エージェントが収集したNIC（ネットワークアダプタ）情報。1エージェントに対して複数NICを保持できる（1対多）。送信のたびに全件削除→再挿入で更新される。

**カラム定義**

| カラム名 | 型 | NULL | デフォルト | 制約 | 説明 |
|---------|-----|:----:|-----------|------|------|
| `id` | bigint | NO | AUTO_INCREMENT | PK | 内部ID |
| `agent_number` | varchar(50) | YES | NULL | FK / INDEX | 対象エージェント番号（`agents.agent_number`） |
| `nic_name` | varchar(100) | YES | NULL | — | NIC名（アダプタ名） |
| `ip_address` | varchar(45) | YES | NULL | — | IPアドレス（IPv4/IPv6対応） |
| `mac_address` | varchar(17) | YES | NULL | — | MACアドレス（現バージョンでは "N/A" 固定） |
| `collected_at` | datetime | YES | NULL | — | 情報収集日時 |

**外部キー**

| 制約名 | カラム | 参照先 | ON DELETE |
|--------|--------|--------|-----------|
| `fk_network_agent` | `agent_number` | `agents.agent_number` | CASCADE |

---

### 4.14 `pc_software_info` — インストールソフトウェア情報

エージェントが収集したインストール済みソフトウェアの一覧。1エージェントに対して多数のレコードが存在する（1対多）。送信のたびに全件削除→再挿入で更新される。

**カラム定義**

| カラム名 | 型 | NULL | デフォルト | 制約 | 説明 |
|---------|-----|:----:|-----------|------|------|
| `id` | bigint | NO | AUTO_INCREMENT | PK | 内部ID |
| `agent_number` | varchar(50) | YES | NULL | FK / INDEX | 対象エージェント番号（`agents.agent_number`） |
| `software_name` | varchar(300) | NO | — | — | **ソフトウェア名** |
| `version` | varchar(100) | YES | NULL | — | バージョン |
| `publisher` | varchar(200) | YES | NULL | — | 発行元・パブリッシャー名 |
| `install_date` | date | YES | NULL | — | インストール日（取得できる場合のみ） |
| `collected_at` | datetime | YES | NULL | — | 情報収集日時 |

**外部キー**

| 制約名 | カラム | 参照先 | ON DELETE |
|--------|--------|--------|-----------|
| `fk_software_agent` | `agent_number` | `agents.agent_number` | CASCADE |

---

### 4.15 `software_master` — ソフトウェアライセンスマスタ

会社が保有するソフトウェアライセンスの管理テーブル。`pc_software_info`（エージェント収集）と照合してライセンス超過を検知することを想定している。

**カラム定義**

| カラム名 | 型 | NULL | デフォルト | 制約 | 説明 |
|---------|-----|:----:|-----------|------|------|
| `id` | bigint | NO | AUTO_INCREMENT | PK | 内部ID |
| `software_name` | varchar(300) | NO | — | — | **ソフトウェア名** |
| `publisher` | varchar(200) | YES | NULL | — | 発行元 |
| `license_type` | varchar(100) | YES | NULL | — | ライセンス種別（例: 年間サブスクリプション、永続ライセンス） |
| `purchased_count` | int | NO | `0` | — | **購入ライセンス数** |
| `note` | varchar(2000) | YES | NULL | — | 備考 |
| `created_at` | datetime | NO | CURRENT_TIMESTAMP | — | 登録日時 |
| `updated_at` | datetime | NO | CURRENT_TIMESTAMP | ON UPDATE | 最終更新日時 |

---

### 4.16 `operation_logs` — 操作ログ

管理画面での操作を記録する監査ログテーブル。Spring AOPの `@Loggable` アノテーションにより自動記録される。

**カラム定義**

| カラム名 | 型 | NULL | デフォルト | 制約 | 説明 |
|---------|-----|:----:|-----------|------|------|
| `id` | bigint | NO | AUTO_INCREMENT | PK | 内部ID |
| `user_id` | bigint | YES | NULL | — | 操作したユーザーID（`users.id` 参照。FK制約なし） |
| `username` | varchar(100) | YES | NULL | — | 操作したユーザー名（ログ時点の値を保持） |
| `operation` | varchar(50) | NO | — | — | 操作種別（例: `CREATE`, `UPDATE`, `DELETE`） |
| `target_type` | varchar(100) | YES | NULL | — | 操作対象のリソース種別（例: `PcAsset`, `Rental`） |
| `target_id` | bigint | YES | NULL | — | 操作対象のID |
| `detail` | varchar(4000) | YES | NULL | — | 操作詳細（変更内容など） |
| `ip_address` | varchar(45) | YES | NULL | — | 操作元IPアドレス |
| `created_at` | datetime | NO | CURRENT_TIMESTAMP | — | 操作日時 |

> **注**: `user_id` はFK制約なし。ユーザー削除後もログが残るよう意図的にFK制約を外している。

---

### 4.17 `code_master` — コードマスタ（汎用）

システム全体で使用するコード値（ステータス・区分・ロール等）を一元管理するマスタテーブル。`code_type` でカテゴリを分類し、各カテゴリ内でコード値とその表示ラベルを保持する。アプリケーション側のハードコードを排除し、画面表示ラベルの変更をDBのみで完結させることが目的。

**カラム定義**

| カラム名 | 型 | NULL | デフォルト | 制約 | 説明 |
|---------|-----|:----:|-----------|------|------|
| `code_type` | varchar(50) | NO | — | PK（複合） | **コード区分**（例: `PC_STATUS`, `ACQUISITION_TYPE`） |
| `code_name` | varchar(100) | NO | — | — | **コード名称**（コード区分の日本語名。例: `PCステータス`, `取得区分`） |
| `code_value` | varchar(50) | NO | — | PK（複合） | **コード値**（他テーブルに実際に格納される値。例: `IN_USE`） |
| `code_label` | varchar(200) | NO | — | — | **表示ラベル**（画面に表示する日本語名。例: `使用中`） |
| `description` | varchar(500) | YES | NULL | — | コード値の補足説明 |
| `sort_order` | int | NO | `0` | — | 同一 `code_type` 内での表示順序番号（1, 2, 3 ... の順に画面表示） |
| `is_active` | tinyint(1) | NO | `1` | — | 有効フラグ（1=有効、0=無効・非表示） |
| `created_at` | datetime | NO | CURRENT_TIMESTAMP | — | 登録日時 |
| `updated_at` | datetime | NO | CURRENT_TIMESTAMP | ON UPDATE | 最終更新日時 |

**code_type の初期登録値**

| code_type | 説明 | 対応カラム |
|-----------|------|-----------|
| `PC_STATUS` | PC資産ステータス | `pc_assets.status` |
| `ACQUISITION_TYPE` | PC取得区分 | `pc_assets.acquisition_type` |
| `USER_ROLE` | システムユーザーロール | `users.role` |
| `AGENT_EVENT_TYPE` | エージェントイベント種別 | `agent_history.event_type` |
| `OPERATION_TYPE` | 操作ログの操作種別 | `operation_logs.operation` |
| `DEVICE_TYPE` | 機器種別 | 機器分類ドロップダウン等で参照 |

**DEVICE_TYPE の区分値**

| code_value | code_label |
|-----------|------------|
| `LAPTOP` | ノートPC（Laptop） |
| `DESKTOP` | デスクトップPC（Desktop） |
| `DISPLAY` | モニター（Display） |
| `KEYBOARD` | キーボード |
| `MOUSE` | マウス |
| `SMARTPHONE` | スマートフォン（iPhone / Android） |
| `TABLET` | タブレット（iPad / Android） |
| `MOBILE_ROUTER` | モバイルルータ（Wi-Fiルータ） |
| `EXTERNAL_STORAGE` | 外付けストレージ（HDD/SSD） |

**インデックス**

| インデックス名 | カラム | 種別 |
|--------------|--------|------|
| PRIMARY | `(code_type, code_value)` | 複合主キー |

> **注**: V9 で `id`（surrogate key）を削除し、`(code_type, code_value)` を主キーに昇格。
> 複合主キーの先頭カラム（`code_type`）が索引として機能するため、`idx_code_master_type` は同時に削除。

---

## 5. マイグレーション履歴

Flywayで管理。`backend/src/main/resources/db/migration/` に格納。

| バージョン | ファイル名 | 内容 |
|-----------|-----------|------|
| V1 | `V1__create_all_tables.sql` | 初期スキーマ作成。全テーブル定義 + 管理者ユーザーの初期データ投入 |
| V2 | `V2__add_user_name_to_pc_assets.sql` | `pc_assets` に `user_name` カラムを追加（エージェント送信の使用者名） |
| V3 | `V3__add_agents_tables.sql` | `agents`・`agent_history` テーブルを新規作成。`pc_assets` に `agent_number` カラムを追加 |
| V4 | `V4__refactor_agent_tables_use_agent_number.sql` | `pc_hardware_info`・`pc_software_info`・`pc_network_info` のリレーションを `pc_asset_id` から `agent_number` ベースに変更 |
| V5 | `V5__allow_duplicate_employee_code.sql` | `employees.employee_code` のUNIQUE制約を廃止（転籍・再入社を考慮） |
| V6 | `V6__create_code_master_table.sql` | `code_master` テーブルを新規作成。PC_STATUS・ACQUISITION_TYPE・USER_ROLE 等の初期コードデータを投入 |
| V7 | `V7__add_code_name_to_code_master.sql` | `code_master` に `code_name`（コード名称・日本語）カラムを追加。`code_type` の次に挿入。既存レコードに初期値を設定 |
| V8 | `V8__add_device_type_to_code_master.sql` | `code_master` に `DEVICE_TYPE`（機器種別）コードを追加。9区分（LAPTOP/DESKTOP/DISPLAY/KEYBOARD/MOUSE/SMARTPHONE/TABLET/MOBILE_ROUTER/EXTERNAL_STORAGE） |
| V9 | `V9__drop_id_from_code_master.sql` | `code_master` から `id` カラムを削除。`(code_type, code_value)` を複合主キーに昇格。`idx_code_master_type` を削除（複合主キー先頭カラムで代替） |

---

## 6. 設計上の決定事項・注意点

### 6.1 エージェント収集テーブルの設計変更（V4）

**変更前（V1〜V3）**
```
pc_hardware_info.pc_asset_id → pc_assets.id
pc_software_info.pc_asset_id → pc_assets.id
pc_network_info.pc_asset_id  → pc_assets.id
```

**変更後（V4〜）**
```
pc_hardware_info.agent_number → agents.agent_number
pc_software_info.agent_number → agents.agent_number
pc_network_info.agent_number  → agents.agent_number
```

**理由**: エージェント番号（`agent_number`）を主識別子にすることで、PC資産IDやホスト名の変更に依存しない安定したリレーションを実現。エージェントが「どのPC資産に紐付くか」は `pc_assets.agent_number` で管理し、収集データはエージェント番号でダイレクトに管理する。

### 6.2 レンタル契約の現行・履歴分離

- **現行**: `pc_acquisition_rental`（1資産1レコード、UNIQUE制約）
- **履歴**: `pc_rental_history`（1資産に対して複数レコード可）

返却処理時に `pc_acquisition_rental` の内容を `pc_rental_history` にコピーしてから現行レコードを更新（`return_date` を設定）する設計。

### 6.3 `operation_logs` のFK制約なし

`operation_logs.user_id` は `users.id` を参照しているが、意図的にFK制約を設けていない。ユーザーが削除された後もログを保持する要件に対応するため。代わりに `username` カラムでログ時点のユーザー名を文字列として保持する。

### 6.4 `employees.employee_code` のUNIQUE制約廃止（V5）

転籍や再入社などで同一社員コードが複数使われるケースに対応するため、V5でUNIQUE制約を廃止してインデックスのみに変更。

### 6.5 ON DELETE CASCADE の適用範囲

PC資産（`pc_assets`）を削除した場合、以下のテーブルのデータも**連鎖削除**される設計。

| テーブル | 連鎖削除 |
|---------|---------|
| `pc_acquisition_purchase` | ✅ CASCADE |
| `pc_acquisition_rental` | ✅ CASCADE |
| `pc_rental_history` | ✅ CASCADE |
| `pc_assignments` | ✅ CASCADE |

エージェント（`agents`）を削除した場合は以下が連鎖削除される。

| テーブル | 連鎖削除 |
|---------|---------|
| `pc_hardware_info` | ✅ CASCADE |
| `pc_network_info` | ✅ CASCADE |
| `pc_software_info` | ✅ CASCADE |

---

### 6.6 `code_master` によるコード値の一元管理（V6）

システム内で使われているコード値（`IN_USE`, `PURCHASE` 等）はこれまでアプリケーション側のEnum定数と `pc_assets.status` 等のカラムにのみ存在していた。V6でコードマスタを導入することで以下を実現する。

- **表示ラベルのDB管理**: 「使用中」「保管中」などの日本語ラベルをDBで管理し、フロントエンドはAPIから取得する。
- **コード拡張の容易化**: 新しいステータスや区分を追加する際、DBへのINSERT + アプリ側のEnum追加のみで対応可能。
- **Enumとの二重管理**: アプリ側のEnum（`PcStatus`, `AcquisitionType` 等）は型安全性確保のために残存させる。DBのコードマスタとの同期はアプリ起動時のチェックで検知する運用を推奨。

---

## 改訂履歴

| 日付 | バージョン | 内容 |
|------|----------|------|
| 2026-05-31 | 1.0 | 初版作成。DBから実データを読み取り全テーブルを定義 |
| 2026-06-03 | 1.1 | V6マイグレーション対応。`code_master` テーブル追加。テーブル数を17に更新 |
| 2026-06-03 | 1.2 | V7マイグレーション対応。`code_master.code_name`（コード名称）カラム追加 |
| 2026-06-03 | 1.3 | V8マイグレーション対応。`code_master` に `DEVICE_TYPE`（機器種別）9区分を追加 |
| 2026-06-03 | 1.4 | V9マイグレーション対応。`code_master` の `id` カラム削除・複合主キー `(code_type, code_value)` への変更 |

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/project3?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Tokyo&allowPublicKeyRetrieval=true&sslMode=DISABLED
    username: root
    password: pass
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    validate-on-migrate: false

logging:
  level:
    com.company.pcmgmt: DEBUG
    org.mybatis: DEBUG
    org.springframework.security: DEBUG

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.company.pcmgmt.domain.mapper.employee.EmployeeMapper">

    <resultMap id="EmployeeResponseMap" type="com.company.pcmgmt.api.dto.response.employee.EmployeeResponse">
        <id     property="id"           column="id"/>
        <result property="employeeCode" column="employee_code"/>
        <result property="fullName"     column="full_name"/>
        <result property="department"   column="department"/>
        <result property="position"     column="position"/>
        <result property="email"        column="email"/>
        <result property="phone"        column="phone"/>
        <result property="location"     column="location"/>
        <result property="isActive"     column="is_active"/>
        <result property="createdAt"    column="created_at"/>
        <result property="updatedAt"    column="updated_at"/>
    </resultMap>

    <sql id="searchWhere">
        <where>
            <if test="req.keyword != null and req.keyword != ''">
                AND (
                    employee_code LIKE CONCAT('%', #{req.keyword}, '%')
                    OR full_name  LIKE CONCAT('%', #{req.keyword}, '%')
                    OR department LIKE CONCAT('%', #{req.keyword}, '%')
                )
            </if>
            <if test="req.isActive != null">
                AND is_active = #{req.isActive}
            </if>
        </where>
    </sql>

    <select id="findAll" resultMap="EmployeeResponseMap">
        SELECT * FROM employees
        <include refid="searchWhere"/>
        ORDER BY employee_code ASC
        LIMIT #{req.size} OFFSET #{req.offset}
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM employees
        <include refid="searchWhere"/>
    </select>

    <select id="findById" resultMap="EmployeeResponseMap">
        SELECT * FROM employees WHERE id = #{id}
    </select>

    <select id="findRawById" resultType="com.company.pcmgmt.domain.entity.Employee">
        SELECT * FROM employees WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO employees (
            employee_code, full_name, department,
            position, email, phone, location, is_active
        ) VALUES (
            #{employeeCode}, #{fullName}, #{department},
            #{position}, #{email}, #{phone}, #{location}, #{isActive}
        )
    </insert>

    <update id="update">
        UPDATE employees SET
            employee_code = #{employeeCode},
            full_name   = #{fullName},
            department  = #{department},
            position    = #{position},
            email       = #{email},
            phone       = #{phone},
            location    = #{location},
            is_active   = #{isActive},
            updated_at  = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <select id="existsByEmployeeCode" resultType="boolean">
        SELECT COUNT(*) > 0 FROM employees
        WHERE employee_code = #{employeeCode}
        <if test="excludeId != null">
            AND id != #{excludeId}
        </if>
    </select>

    <select id="findActiveList" resultMap="EmployeeResponseMap">
        SELECT id, employee_code, full_name, department, position
        FROM employees
        WHERE is_active = true
        ORDER BY employee_code ASC
    </select>

</mapper>

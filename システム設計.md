# システム設計書

> 作成日: 2026-05-31
> 対象プロジェクト: PC管理システム (Project3)

---

## 目次

1. [機能ID設計](#1-機能id設計)
2. [フォルダ構成](#2-フォルダ構成)
3. [ファイル命名規則](#3-ファイル命名規則)
4. [パッケージ設計（Java Backend）](#4-パッケージ設計java-backend)
5. [機能ID一覧](#5-機能id一覧)
6. [コーディング規約](#6-コーディング規約)
7. [新機能追加時のチェックリスト](#7-新機能追加時のチェックリスト)

---

## 1. 機能ID設計

### 1.1 フォーマット

```
[アプリ種別3桁]_[ドメイン3桁][機能連番2桁][機能枝番2桁]

例: WEB_AST0101
    ^^^  ^^^  ^^  ^^
    |    |    |   +-- 機能枝番 (01〜): 画面バリエーション
    |    |    +------ 機能連番 (01〜): 同ドメイン内の機能グループ番号
    |    +----------- ドメイン (3桁): 業務領域
    +---------------- アプリ種別 (3桁): 対象アプリ
```

### 1.2 アプリ種別コード

| コード | 対象アプリ | 説明 |
|--------|-----------|------|
| `WEB`  | front-web  | Vue.js Web管理画面 |
| `AGT`  | agent      | Tauri デスクトップエージェントアプリ |
| `CMN`  | 共通       | AGT・WEB 両方から使用されるバックエンドAPI |

### 1.3 ドメインコード

| コード | 業務領域 | 対象アプリ |
|--------|---------|-----------|
| `AST`  | 資産管理           | WEB |
| `RNT`  | レンタル管理       | WEB |
| `EMP`  | 社員管理           | WEB |
| `USR`  | ユーザー管理       | WEB |
| `SFW`  | ソフトウェア管理   | WEB |
| `LON`  | 貸出管理           | WEB |
| `DSH`  | ダッシュボード     | WEB |
| `AUT`  | 認証               | WEB |
| `EXP`  | エクスポート       | WEB |
| `PCI`  | PC情報収集         | AGT |
| `CFG`  | 設定               | AGT |
| `WUP`  | WindowsUpdate判定  | AGT |
| `AGT`  | エージェント共通API | CMN |

### 1.4 機能枝番の割り当てルール

| 枝番 | 用途 |
|------|------|
| `01` | 一覧 / メイン画面 |
| `02` | 詳細 / モーダル（親画面の子） |
| `03` | 新規登録 |
| `04` | 編集・更新 |
| `05` | 削除 |
| `99` | その他・予備 |

### 1.5 バックエンドとの対応ルール

- **WEB / AGT の機能** に対応するバックエンドは **同一の機能IDを使用する**
  - 例: `WEB_AST0101`（資産一覧） → `PcAssetController` に `WEB_AST0101` と明記
- **CMN（共通）** はバックエンドの機能IDとして独立して管理する
  - 例: `CMN_AGT0101`（エージェント初回登録）
- 1つのコントローラが複数機能IDに対応する場合は **すべて列挙する**
  - 例: `PcAssetController` → `WEB_AST0101 (資産一覧), WEB_AST0102 (資産詳細)`

---

## 2. フォルダ構成

### 2.1 プロジェクト全体構成

```
Project3/
├── frontend-web/          # Web管理画面 (Vue.js + TypeScript + Vite)
├── agent/                 # エージェントアプリ (Vue.js + Tauri + Rust)
├── backend/               # バックエンドAPI (Spring Boot + Java)
├── docs/                  # ドキュメント
│   ├── system-design.md   # 本ファイル
│   ├── progress.md        # 進捗管理
│   ├── 01_issue/          # 不具合記録
│   └── 02_codeChange/     # 仕様変更記録
└── docker-compose.yml
```

### 2.2 frontend-web フォルダ構成

```
frontend-web/src/
├── views/                         # 画面コンポーネント（ルートに対応）
│   ├── WEB_AUT0101_LoginView.vue
│   ├── WEB_DSH0101_DashboardView.vue
│   ├── WEB_AST0101_AssetListView.vue
│   ├── WEB_AST0102_AssetDetailView.vue
│   ├── WEB_RNT0101_RentalListView.vue
│   ├── WEB_EMP0101_EmployeeListView.vue
│   ├── WEB_USR0101_UserListView.vue
│   ├── WEB_SFW0101_SoftwareListView.vue
│   └── WEB_LON0101_LoanListView.vue
├── components/                    # 共通コンポーネント・機能コンポーネント
│   ├── WEB_RNT0102_RentalDetailModal.vue  # 機能専用コンポーネント
│   ├── AppLayout.vue              # 共通レイアウト（機能IDなし）
│   ├── AppPagination.vue          # 共通UI（機能IDなし）
│   └── StatusBadge.vue            # 共通UI（機能IDなし）
├── api/                           # バックエンドAPI通信
├── stores/                        # Pinia ストア
├── router/
│   └── index.ts                   # ルーティング設定
├── types/
│   └── index.ts                   # 型定義
└── image/                         # アイコン画像（SVG）
```

#### ルールのポイント

- **views/ 配下のファイル** は必ず機能IDプレフィックスを付ける
- **components/ 配下** は「特定機能専用コンポーネント」は機能IDを付け、「共通UIコンポーネント」は機能IDなし
- **api/, stores/, types/** は機能IDを付けない（ドメイン名ベースのファイル名）

### 2.3 agent フォルダ構成

```
agent/
├── src/
│   └── App.vue            # メインコンポーネント（複数機能IDを含むため単一ファイル）
├── src-tauri/
│   └── src/
│       ├── lib.rs          # コアロジック（複数機能IDを含むため単一ファイル）
│       └── main.rs
└── image/                  # アイコン画像（SVG）
```

> **注記**: agentは単一ページアプリのため、ファイルのリネームは行わない。
> 機能IDはファイル内のヘッダーコメントで管理する。

### 2.4 backend フォルダ構成

```
backend/src/main/java/com/company/pcmgmt/
├── api/
│   ├── controller/
│   │   ├── auth/           # WEB_AUT0101
│   │   ├── dashboard/      # WEB_DSH0101
│   │   ├── asset/          # WEB_AST0101, WEB_AST0102
│   │   ├── rental/         # WEB_RNT0101, WEB_RNT0102
│   │   ├── employee/       # WEB_EMP0101
│   │   ├── user/           # WEB_USR0101
│   │   ├── software/       # WEB_SFW0101
│   │   ├── loan/           # WEB_LON0101
│   │   ├── export/         # WEB_EXP0101
│   │   └── agent/          # CMN_AGT0101, CMN_AGT0102, CMN_AGT0103
│   └── dto/
│       ├── request/
│       │   ├── auth/       # LoginRequest
│       │   ├── asset/      # AssetCreateRequest, AssetSearchRequest, AssetUpdateRequest
│       │   ├── rental/     # RentalCreateRequest, RentalSearchRequest, ...
│       │   ├── employee/   # EmployeeCreateRequest, ...
│       │   ├── user/       # UserCreateRequest, UserUpdateRequest
│       │   ├── software/   # SoftwareCreateRequest, SoftwareSearchRequest
│       │   ├── loan/       # LoanCreateRequest, LoanReturnRequest, LoanSearchRequest
│       │   └── agent/      # AgentRegisterRequest, AgentReportRequest
│       └── response/
│           ├── auth/       # LoginResponse
│           ├── dashboard/  # DashboardStatsResponse
│           ├── asset/      # AssetResponse, InstalledSoftwareResponse
│           ├── rental/     # RentalResponse, RentalVendorResponse
│           ├── employee/   # EmployeeResponse
│           ├── user/       # UserResponse
│           ├── software/   # SoftwareResponse
│           ├── loan/       # LoanResponse
│           ├── agent/      # AssetInfoResponse
│           ├── ApiResponse.java    # 【共通】全レスポースのラッパー（移動しない）
│           └── PageResponse.java   # 【共通】ページネーション（移動しない）
├── service/
│   ├── auth/           # AuthService
│   ├── dashboard/      # DashboardService
│   ├── asset/          # PcAssetService
│   ├── rental/         # RentalService
│   ├── employee/       # EmployeeService
│   ├── user/           # UserService
│   ├── software/       # SoftwareService
│   ├── loan/           # LoanService
│   ├── export/         # ExportService
│   ├── agent/          # AgentService
│   └── OperationLogService.java  # 【横断的関心事】移動しない
├── domain/
│   ├── entity/         # エンティティ（ドメイン別に分けない）
│   └── mapper/
│       ├── asset/      # PcAssetMapper.java
│       ├── rental/     # RentalMapper.java
│       ├── employee/   # EmployeeMapper.java
│       ├── user/       # UserMapper.java
│       ├── software/   # SoftwareMapper.java
│       ├── loan/       # LoanMapper.java
│       ├── agent/      # AgentMapper.java
│       └── OperationLogMapper.java  # 【横断的関心事】移動しない
├── security/           # JWT認証（横断的関心事・移動しない）
├── config/             # Spring設定（横断的関心事・移動しない）
├── exception/          # 例外処理（横断的関心事・移動しない）
├── interceptor/        # ロギングインターセプター（横断的関心事・移動しない）
├── annotation/         # カスタムアノテーション（横断的関心事・移動しない）
└── constant/           # 定数（横断的関心事・移動しない）

backend/src/main/resources/
└── com/company/pcmgmt/domain/mapper/
    ├── asset/          # PcAssetMapper.xml
    ├── rental/         # RentalMapper.xml
    ├── employee/       # EmployeeMapper.xml
    ├── user/           # UserMapper.xml
    ├── software/       # SoftwareMapper.xml
    ├── loan/           # LoanMapper.xml
    ├── agent/          # AgentMapper.xml
    └── OperationLogMapper.xml  # 横断的関心事
```

#### ドメイン分割の原則

| 分類 | 対象 | 理由 |
|------|------|------|
| **ドメイン別サブフォルダに配置** | controller, service, mapper, dto | 機能の所属を明確にするため |
| **共通として残す** | `ApiResponse`, `PageResponse` | 全ドメインから参照されるため |
| **横断的関心事として残す** | security, config, exception, interceptor, annotation, constant, OperationLog系 | 特定ドメインに属さないため |
| **entity は分けない** | domain/entity/ | エンティティ間の関連が強く、分割するメリットより複雑さが増すため |

---

## 3. ファイル命名規則

### 3.1 frontend-web

#### Viewファイル（views/）

```
{機能ID}_{元のファイル名}.vue

例:
  WEB_AST0101_AssetListView.vue
  WEB_AST0102_AssetDetailView.vue
  WEB_RNT0101_RentalListView.vue
  WEB_RNT0102_RentalDetailModal.vue
```

#### コンポーネントファイル（components/）

```
【機能専用コンポーネント】 {機能ID}_{コンポーネント名}.vue
【共通UIコンポーネント】   {コンポーネント名}.vue（機能IDなし）

例:
  WEB_RNT0102_RentalDetailModal.vue  # レンタル詳細専用
  AppLayout.vue                       # 全画面共通レイアウト
  AppPagination.vue                   # 共通ページネーション
  StatusBadge.vue                     # 共通ステータス表示
```

#### APIファイル（api/）

```
{ドメイン名（英小文字）}.ts

例:
  assets.ts
  rentals.ts
  employees.ts
```

### 3.2 backend（Java）

#### ファイル名のルール

- **Javaはファイル名 = public クラス名** が必須のため、機能IDプレフィックスは付けない
- 機能IDはファイル先頭の **Javadocコメント** で管理する

```java
/**
 * 機能ID: WEB_AST0101 (資産一覧), WEB_AST0102 (資産詳細)
 * 機能名: 資産管理
 */
package com.company.pcmgmt.api.controller.asset;

public class PcAssetController { ... }
```

#### パッケージ命名規則

```
com.company.pcmgmt.{レイヤー}.{ドメイン名（英小文字）}

例:
  com.company.pcmgmt.api.controller.asset
  com.company.pcmgmt.service.rental
  com.company.pcmgmt.domain.mapper.employee
  com.company.pcmgmt.api.dto.request.loan
  com.company.pcmgmt.api.dto.response.asset
```

#### ドメイン名（フォルダ名）の対応表

| ドメインコード | フォルダ名 | 説明 |
|--------------|-----------|------|
| AST | `asset` | 資産管理 |
| RNT | `rental` | レンタル管理 |
| EMP | `employee` | 社員管理 |
| USR | `user` | ユーザー管理 |
| SFW | `software` | ソフトウェア管理 |
| LON | `loan` | 貸出管理 |
| DSH | `dashboard` | ダッシュボード |
| AUT | `auth` | 認証 |
| EXP | `export` | エクスポート |
| AGT（CMN） | `agent` | エージェント共通API |

### 3.3 MyBatis XMLマッパー

```
resources/com/company/pcmgmt/domain/mapper/{ドメイン名}/{クラス名}Mapper.xml

例:
  mapper/asset/PcAssetMapper.xml
  mapper/rental/RentalMapper.xml
```

- `application.yml` の `mapper-locations` は `**/*.xml`（再帰検索）を使用する

```yaml
mybatis:
  mapper-locations: classpath:com/company/pcmgmt/domain/mapper/**/*.xml
```

---

## 4. パッケージ設計（Java Backend）

### 4.1 レイヤー構成と依存方向

```
[Controller] → [Service] → [Mapper] → [Entity]
     ↓               ↓
   [Request DTO]  [Response DTO]
```

- **Controller** → Service・Request DTO・Response DTO を参照
- **Service** → Mapper・Entity・Request DTO・Response DTO を参照
- **Mapper** → Entity を参照
- 上位レイヤーが下位レイヤーを参照（逆方向参照禁止）

### 4.2 共通クラスの扱い

以下は特定ドメインに属さないため、サブフォルダに移動しない。

| クラス | パッケージ | 理由 |
|--------|-----------|------|
| `ApiResponse<T>` | `api.dto.response` | 全コントローラーが使用 |
| `PageResponse<T>` | `api.dto.response` | 全一覧APIが使用 |
| `OperationLogService` | `service` | 横断的なログ記録 |
| `OperationLogMapper` | `domain.mapper` | 横断的なログ記録 |

### 4.3 新ドメイン追加時のパッケージ作成手順

1. 機能IDを決定する（本設計書 §1 参照）
2. 以下のフォルダを同時に作成する

```
api/controller/{domain}/
api/dto/request/{domain}/
api/dto/response/{domain}/
service/{domain}/
domain/mapper/{domain}/
resources/.../mapper/{domain}/
```

3. `package` 宣言を新パッケージ名で記述する
4. ファイル先頭に機能IDコメントを記述する

---

## 5. 機能ID一覧

### WEB（front-web）

| 機能ID | 機能名 | Vueファイル | 対応バックエンド |
|--------|--------|------------|----------------|
| `WEB_AUT0101` | ログイン | `WEB_AUT0101_LoginView.vue` | `AuthController` |
| `WEB_DSH0101` | ダッシュボード | `WEB_DSH0101_DashboardView.vue` | `DashboardController` |
| `WEB_AST0101` | 資産一覧 | `WEB_AST0101_AssetListView.vue` | `PcAssetController` |
| `WEB_AST0102` | 資産詳細 | `WEB_AST0102_AssetDetailView.vue` | `PcAssetController` |
| `WEB_RNT0101` | レンタル一覧 | `WEB_RNT0101_RentalListView.vue` | `RentalController` |
| `WEB_RNT0102` | レンタル詳細（モーダル） | `WEB_RNT0102_RentalDetailModal.vue` | `RentalController` |
| `WEB_EMP0101` | 社員一覧 | `WEB_EMP0101_EmployeeListView.vue` | `EmployeeController` |
| `WEB_USR0101` | ユーザー一覧 | `WEB_USR0101_UserListView.vue` | `UserController` |
| `WEB_SFW0101` | ソフトウェア一覧 | `WEB_SFW0101_SoftwareListView.vue` | `SoftwareController` |
| `WEB_LON0101` | 貸出一覧 | `WEB_LON0101_LoanListView.vue` | `LoanController` |
| `WEB_EXP0101` | CSV/Excelエクスポート | 各画面に組み込み | `ExportController` |

### AGT（agent）

| 機能ID | 機能名 | 実装箇所 | 対応バックエンド |
|--------|--------|---------|----------------|
| `AGT_PCI0101` | PC情報表示タブ | `App.vue` info view | ―（ローカル収集） |
| `AGT_PCI0102` | PC情報送信タブ | `App.vue` send view | `CMN_AGT0103` |
| `AGT_CFG0101` | 設定タブ | `App.vue` settings view | `CMN_AGT0101〜0103` |
| `AGT_CFG0102` | 新規登録確認モーダル | `App.vue` modal | `CMN_AGT0101` |
| `AGT_WUP0101` | WindowsUpdate適用判定 | `lib.rs` | ―（ローカル実行） |

### CMN（共通API）

| 機能ID | API機能名 | エンドポイント | 利用元 |
|--------|----------|--------------|--------|
| `CMN_AGT0101` | エージェント初回登録 | POST `/agent/register` | `AGT_CFG0102` |
| `CMN_AGT0102` | 資産情報取得（取得区分・返却状況） | GET `/agent/asset-info` | `AGT_CFG0101` |
| `CMN_AGT0103` | PC情報レポート送信 | POST `/agent/report` | `AGT_PCI0102` |

---

## 6. コーディング規約

### 6.1 機能IDコメントの記述ルール

#### Vue / TypeScript

```vue
<!--
  機能ID: WEB_AST0101
  views/WEB_AST0101_AssetListView.vue
  -----------------------------------------------
  資産一覧画面コンポーネント
  -----------------------------------------------
-->
```

#### Java（Controller）

```java
/**
 * 機能ID: WEB_AST0101 (資産一覧), WEB_AST0102 (資産詳細)
 * 機能名: 資産管理
 */
package com.company.pcmgmt.api.controller.asset;
```

#### Rust（agent lib.rs）

```rust
/// 機能ID: AGT_PCI0101
/// 機能名: PC情報収集
```

### 6.2 ルーター（router/index.ts）

ルートにはコメントで機能IDを明記する。

```typescript
{
  path: '/assets',
  name: 'AssetList',
  // PC 資産一覧 (WEB_AST0101)
  component: () => import('@/views/WEB_AST0101_AssetListView.vue'),
  meta: { requiresAuth: true },
},
```

### 6.3 命名規則（全般）

| 対象 | 規則 | 例 |
|------|------|-----|
| 変数・関数 | lowerCamelCase | `assetNumber`, `loadRentals()` |
| 定数 | lowerCamelCase | `pageSize` |
| クラス名（Java） | PascalCase | `PcAssetController` |
| インターフェース（Java） | PascalCase | `PcAssetMapper` |
| APIエンドポイント | kebab-case | `/api/v1/pc-assets` |
| Boolean変数 | is/has/can接頭辞 | `isLoading`, `hasError` |
| ドメイン用フォルダ名（Java） | 英小文字 | `asset`, `rental` |
| 機能IDプレフィックス（Vueファイル） | 大文字+アンダースコア | `WEB_AST0101_` |

### 6.4 禁止事項

| 禁止 | 理由 | 代替 |
|------|------|------|
| `any` 型の使用 | 型安全性が破壊される | `unknown` で受けて型ガード |
| リストの `key` に `index` を使用 | 並び替え・削除時にバグが起きる | 一意のIDを使用 |
| 1ファイル300行以上 | レビューが困難 | コンポーネント分割・カスタムフック抽出 |
| env変数のハードコード | セキュリティリスク | `.env` + 環境変数経由で参照 |
| アイコンをコードで描画 | 規約違反 | `image/` フォルダの SVG ファイルを `<img>` で読み込む |

### 6.5 アイコン使用規約

- アイコンは **画像ファイル（SVG推奨）** として `image/` フォルダに格納する
- 各コンポーネントは `<img src="./image/icon-xxx.svg" />` または `<img src="@/image/icon-xxx.svg" />` で読み込む
- コード内でのアイコン描画（Font Awesome, Material Icons のクラス指定等）は使用しない

---

## 7. 新機能追加時のチェックリスト

### 7.1 機能追加前

- [ ] 機能IDを決定したか（本設計書 §1 参照）
- [ ] 既存の機能IDと重複していないか
- [ ] 親子関係のある画面は枝番で表現されているか

### 7.2 frontend-web に画面を追加する場合

- [ ] `views/` に `{機能ID}_{機能名}View.vue` の形式でファイルを作成したか
- [ ] ファイル先頭に機能IDコメントを記述したか
- [ ] `router/index.ts` にルートを追加し、機能IDコメントを付けたか
- [ ] 対応するAPIファイルを `api/{ドメイン}.ts` に追加したか

### 7.3 backend に機能を追加する場合

- [ ] 正しいドメインサブフォルダに配置したか
  - `api/controller/{domain}/`
  - `api/dto/request/{domain}/`
  - `api/dto/response/{domain}/`
  - `service/{domain}/`
  - `domain/mapper/{domain}/`
- [ ] `package` 宣言がフォルダ構成と一致しているか
  - `package com.company.pcmgmt.api.controller.asset;`
- [ ] ファイル先頭に機能IDのJavadocコメントを記述したか
- [ ] MyBatis XMLを追加した場合、`resources/.../mapper/{domain}/` に配置したか

### 7.4 新ドメインを追加する場合

- [ ] ドメインコード（3桁英大文字）を決定し、本設計書の §1.3 ドメインコード表に追記したか
- [ ] ドメイン名（英小文字）を決定し、本設計書の §3.2 対応表に追記したか
- [ ] backend の全レイヤーにサブフォルダを作成したか
- [ ] 機能ID一覧（§5）に追記したか

---

## 改訂履歴

| 日付 | バージョン | 内容 |
|------|----------|------|
| 2026-05-31 | 1.0 | 初版作成。命名規則・フォルダ構成・機能IDを策定 |
